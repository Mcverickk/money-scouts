services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: edge_desk
    ports:
      - '5432:5432'
    volumes:
      - edge-desk-postgres:/var/lib/postgresql/data

volumes:
  edge-desk-postgres:
