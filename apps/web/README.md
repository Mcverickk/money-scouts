# apps/web — management UI, run trace, scoreboard

Not scaffolded yet — **unowned** (see hackathon-context/TEAM_AND_CONSTRAINTS.md). Scope per
docs/TECH_ARCHITECTURE.md §4.1 and §4.10: one dashboard page (add/pause markets, edit
thresholds, inspect a run end-to-end) plus the public scoreboard (Cloudflare Pages).

Before building from scratch: `hermes dashboard` / `hermes kanban watch` may already cover
the run-trace view (docs/HERMES_INTEGRATION.md) — the scoreboard may be the only real build.
