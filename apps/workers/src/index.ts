import 'dotenv/config';

// Worker entry point. Each worker is a module here; for the buildathon they can all
// run inside this one process (docs/TECH_ARCHITECTURE.md §8).
console.log('edge-desk workers: no workers wired up yet (see src/*.ts stubs)');
